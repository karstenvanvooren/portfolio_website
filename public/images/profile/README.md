# Profile photo

Drop your photo in this folder named exactly:

```
photo.jpg
```

(`.jpeg`, `.png`, or `.webp` also work — just name the file `photo.<ext>`.)

It's picked up automatically on the About page and the homepage "About"
panel — no code changes needed. Until it's here, both show the orange
gradient placeholder.
